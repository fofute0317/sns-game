# Vercel + Supabase への移行メモ

自前の Node.js サーバ（HTTP + WebSocket + ローカルJSON保存）で動いていたものを、
**Vercel Functions + Supabase（Postgres / Realtime）** に載せ替えました。

**ゲームのルール・計算・画面は変えていません。**
変えたのは「どこで動かすか」と「状態をどこに置くか」だけです。

---

## 1. なぜ載せ替えが必要だったか

Vercel は「リクエストが来たら関数を起動し、返したら捨てる」仕組みです。
旧実装は、その正反対の前提で書かれていました。

| 旧実装がしていたこと | Vercel でできない理由 |
| --- | --- |
| `server.listen(3000)` でサーバを常駐させる | 常駐プロセスを置けない |
| `/ws` で WebSocket を張りっぱなしにする | 関数の実行が終わると接続も切れる |
| `data/rooms.json` に書き込む | デプロイ後のファイルは読み取り専用 |
| `Map<code, Room>` をメモリに持つ | リクエストごとに別インスタンス。共有されない |
| `setTimeout` で制限時間を締め切る | 関数の外でタイマーを動かせない |

---

## 2. 置き換えの対応表

| 旧 | 新 | 備考 |
| --- | --- | --- |
| `server/index.js`（HTTP + ルーティング） | `app/api/**/route.ts` | Vercel Functions |
| `server/ws.js`（WebSocketサーバ） | **削除** | Supabase Realtime へ |
| `public/js/net.js`（WebSocketクライアント） | `lib/realtime.ts` | 外から見えるAPIは同一 |
| `server/store.js`（Map + rooms.json） | `lib/store.ts` + `rooms` テーブル | 楽観ロック付き |
| `server/room.js`（`class Room`） | `lib/game.ts`（純粋関数） | 状態は引数で受け取る |
| `server/rules.js`（`fs` で config を読む） | `lib/rules.ts`（JSONを import） | ファイルシステム不要 |
| `server/bots.js` | `lib/bots.ts` | 逐語移植 |
| `shared/engine.js` / `shared/rng.js` | `lib/engine.ts` / `lib/rng.ts` | 逐語移植（計算式は同一） |
| `public/*.html` | `app/**/page.tsx` + `client/markup/*.ts` | マークアップは原文のまま |
| `public/js/*.js`（画面ロジック） | `client/*.js` | import 先だけ変更 |
| `public/css/*.css` | `styles/*.css` | 変更なし |
| `setInterval(sweep, 10分)` | `/api/cron/sweep` + Vercel Cron | 毎時 |

---

## 3. 設計上の判断（なぜこう作ったか）

### 3-1. 状態は「1行のJSONB」に置く

`rooms.game_state` に、旧 `Room.toJSON()` と同じ形の状態がまるごと入ります。

**プレイヤーの一覧も、その中に入っています。**
`players` テーブルにも同じ内容が入りますが、そちらは**分析・集計のための写し**で、
ゲーム進行はそちらを読みません。

理由は **アトミック性** です。サーバレスでは
「5人が同時に決定ボタンを押す」ことが日常的に起きます。
状態が複数の行に散っていると、トランザクションなしでは一部だけ更新された中途半端な状態が生まれます。
1行に閉じていれば、その行の更新が成功したかどうかだけを見れば済みます。

### 3-2. 同時更新は楽観ロック（version の CAS）で守る

```sql
UPDATE rooms
   SET game_state = ..., version = version + 1
 WHERE id = ? AND version = ?;     -- ← 読んだときの version と一致する場合だけ
```

0件しか更新されなければ「誰かが先に書いた」ということなので、
**読み直して最初からやり直します**（`lib/store.ts` の `mutateRoom`、最大6回）。

やり直しても安全なのは、`lib/game.ts` が純粋関数だからです。
副作用（配信・ログ・写しの更新）は、保存が成功したあとにだけ実行します。

### 3-3. Realtime では「状態」を配らず「合図」だけを配る

`room:{roomCode}` チャンネルに流すのは、イベント名と `{ roomCode, version, at }` だけです。
各ブラウザは合図を受け取ると、自分のトークンを付けて `/api/rooms/state` を取りに行きます。

**なぜ状態そのものを流さないか**: 生徒ごとに見えてよい情報が違うためです。
決定フェーズ中、他人がどの選択肢を選んだかは見えてはいけません。
全員に同じ本文を配ると、開発者ツールから他人の選択が読めてしまいます。

往復が1回増えますが、実測で数十ミリ秒です。授業では体感できません。

サーバからの配信は WebSocket ではなく **Realtime の HTTP broadcast エンドポイント**を使います。
サーバレス関数で WebSocket を張ると、接続確立だけで数百ミリ秒かかるうえ、
関数が終わると切断されてしまうためです。

### 3-4. 制限時間：タイマーを「時刻」に置き換えた

旧: `setTimeout(() => room.forceResolve('time'), timerSec * 1000)`
新: `deadline`（締め切り時刻）を状態に保存し、**リクエストのたびに経過を判定**する

- `mutateRoom` は、どの操作よりも先に `tickDeadline()` を通します。
  → 誰かが何か操作すれば、その時点で必ず締め切られます。
- 誰も操作しないまま時間が過ぎる場合に備え、**先生の画面が締め切り時刻に
  `/api/game/tick` を1回叩きます**。
- 締め切るかどうかを判断するのは常にサーバです。
  クライアントが早く叩いても、時刻が来ていなければ何も起きません。

### 3-5. 練習用AI：思考時間を廃止し、その場で確定する

旧: 決定フェーズに入ると `setTimeout`（1.2〜3.8秒）でAIが提出
新: 決定フェーズに入った時点で、その場でAIの手を確定

AIの「考えている時間」は演出であり、ゲームの結果には影響しません。

**選ぶ手は旧実装と完全に同一です。** 乱数列を揃えるため、
戻り値を捨てるとわかっていても `botThinkDelay(rng)` を同じ順序で必ず1回呼んでいます
（これを省くと、続く `decideForBot` が引く乱数がずれて別の手を選んでしまいます）。

### 3-6. 画面：マークアップを原文のまま持ち込んだ

`app/**/page.tsx` は、次の3つだけを行います。

1. 元の HTML（`client/markup/*.ts` の文字列）を1回だけ置く
2. `<body>` に元と同じクラスを付ける（CSS が `body.screen-*` を見ているため）
3. 既存のコントローラ（`client/teacher.js` / `client/play.js`）を起動する

この教材の画面は、状態を見て DOM を直接組み立てる作りです（描画だけで1,100行以上）。
React コンポーネントへ書き直しても React は再描画に関与しないため利点がなく、
属性の書き換えミスによる**見た目の崩れだけが確実に増えます**。

移行の目的はインフラの載せ替えであり、描画方式の変更ではないため、
マークアップと画面ロジックはそのまま残しました。**移行前後で画面は完全に同一です。**

`test/markup.test.js` が、移行前の HTML にあった id がすべて残っていることを検証します。

### 3-7. クライアントの通信層は「差し替え」だけで済ませた

`lib/realtime.ts` の `Net` クラスは、旧 `public/js/net.js` と
**外から見えるAPIがまったく同じ**です。

```js
net.on('welcome' | 'state' | 'error' | 'kicked' | 'roomClosed' | 'status' | ...)
net.send({ t: 'joinRoom' | 'submit' | 'start' | 'next' | ... })
net.loadBackupSession() / net.clearSession() / net.connect()
```

中身だけが「WebSocket 1本」から「HTTP POST + Realtime購読」に変わっています。
おかげで `client/teacher.js`（498行）と `client/play.js`（625行）は、
**import 文1行を書き換えただけ**で動いています。

---

## 4. 変わらなかったもの（意図的に）

- フェアトレードゲームのルール一式（`config/rules.*.json` は無変更）
- 需要・売上・利益・点数の計算式（`lib/engine.ts` は逐語移植）
- 総合得点の重み付けと正規化、ランキングの同順位の扱い
- イベント（カカオ不作・SNSで話題 等）とその効果
- 会社データ（`config/companies.json`）
- 練習用AIの4戦略と、その強さ
- 先生用コンソールの操作（開始・締め切り・次へ・戻る・AI追加・退出・やり直し・CSV出力・投影モード）
- 生徒用画面の操作（参加・選び直し・決定・取り消し・再接続）
- QRコード生成（`client/qr.js`）と短縮URL `/j/123456`
- 画面の見た目（HTML・CSS は無変更）

---

## 5. 変わったこと（利用者から見て）

| 項目 | 旧 | 新 |
| --- | --- | --- |
| URL | `/teacher.html` `/play.html` | `/teacher` `/play`（旧URLは自動転送） |
| お知らせの更新 | `config/news.json` を編集すれば即反映 | 編集後に再デプロイが必要 |
| ルール追加 | `config/` にJSONを置くだけ | JSONを置き、`lib/rules.ts` に1行追加 |
| AIの提出 | 1.2〜3.8秒かけて提出 | すぐに提出済みになる |
| 反映の速さ | WebSocketで即時 | 合図＋再取得（数十ms増） |
| 起動時のルール検証 | 壊れていたらサーバが起動しない | `npm test` と `/api/rulesets` で検知 |

### 見落としやすい3点（レビューで確認した挙動の差）

**1. 参加者が全員AIのルームは、決定画面を経由しません**

AIは決定フェーズに入った時点で提出済みになるため、全員がAIだと
「開始」を押した瞬間にラウンドが解決し、いきなり結果画面になります。
旧実装では2秒ほど決定画面が表示されていました。

生徒が1人でもいれば影響はありません（その生徒の提出を待ちます）。
先生が1台で予行演習するときだけ、テンポが速くなります。

**2. 同じ端末での二重接続を弾かなくなりました（`replaced` 通知の廃止）**

旧実装は接続を1本ずつ管理していたため、先生用コンソールを2つ開くと
古いほうに「別の画面で開かれました」と出して切断していました。

HTTP + トークン方式には「切断すべき接続」が存在しないため、
2つのタブが両方とも同じルームを操作できます。
状態はサーバに1つしかないので**表示が食い違うことはなく**、
むしろ「プロジェクタ用と手元用で2画面出す」使い方ができるようになっています。

**3. レート制限がありません**

旧実装は1接続あたり「5秒で60メッセージ」を超えると切断していました。
サーバレスではリクエストをまたいだ回数の記憶ができないため、この機能はありません。

操作にはそのルームのトークンが必要なので、影響を及ぼせるのは
すでにルームに入っている参加者だけです（無関係な第三者は何もできません）。
厳格に制限したい場合は Vercel の Firewall、または
Upstash などの外部ストアを使ったレート制限を追加してください。

---

## 6. 削除したファイルと、その理由

| 削除したもの | 理由 |
| --- | --- |
| `server/` 一式 | Vercel Functions に置き換え |
| `data/rooms.json` | Postgres に置き換え |
| `test/e2e.test.js` `test/protocol.test.js` `test/helpers.js` | WebSocketサーバを起動して検証するテスト。対象が無くなった |
| `test/client-*.test.js` | 同上（`startServer` に依存）＋旧HTMLファイルを直接読んでいた |

**代わりに追加したもの**

- `test/game.test.js` … 旧 e2e と同じ検証項目を、状態遷移そのものに対して実施（32件）
- `test/markup.test.js` … 移行前のHTMLの id・構造がすべて残っているかを検証（11件）

テストは移行前 57件 → 移行後 **93件** です。

---

## 7. 引き継ぎメモ

- ゲームのルールを変えたい → `config/rules.*.json` だけを触る（旧と同じ）
- 計算式を変えたい → `lib/engine.ts`
- 進行（フェーズ遷移）を変えたい → `lib/game.ts`
- 通信を変えたい → `lib/realtime.ts`（クライアント）と `app/api/`（サーバ）
- 画面を変えたい → `client/markup/*.ts`（構造）と `client/*.js`（描画）と `styles/*.css`（見た目）
- DBを変えたい → `supabase/schema.sql` と `lib/store.ts`
